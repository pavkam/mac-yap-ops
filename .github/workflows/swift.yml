# SPDX-FileCopyrightText: 2026 Alexandru Ciobanu (alex+git@ciobanu.org)
# SPDX-License-Identifier: MIT

name: "🎙️ Swift CI"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  repository-quality:
    name: "⚖️ Repository quality"
    runs-on: macos-latest
    timeout-minutes: 5

    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v7

      - name: "⚖️ Verify licensing, structure, and DocC"
        run: make check

  build-and-test:
    name: "🧪 Build & test"
    runs-on: macos-latest
    timeout-minutes: 15

    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v7

      - name: "🧭 Show toolchain"
        run: |
          swift --version
          xcodebuild -version

      - name: "📦 Resolve dependencies"
        run: swift package resolve

      - name: "🔨 Build application and tests"
        run: swift build --build-tests -Xswiftc -warnings-as-errors -v

      - name: "✅ Run tests"
        run: swift test --skip-build

  thread-sanitizer:
    name: "🧵 Thread sanitizer"
    runs-on: macos-latest
    timeout-minutes: 15

    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v7

      - name: "🧵 Run tests with Thread Sanitizer"
        run: swift test --sanitize=thread

  package-app:
    name: "📦 Package app"
    needs: [repository-quality, build-and-test, thread-sanitizer]
    runs-on: macos-latest
    timeout-minutes: 15

    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v7

      - name: "🧱 Build signed app bundle"
        env:
          SIGN_IDENTITY: "-"
        run: make app

      - name: "🔍 Verify app bundle"
        run: |
          test -x .build/YapOps.app/Contents/MacOS/YapOps
          test -f .build/YapOps.app/Contents/Resources/YapOps.icns
          test -f .build/YapOps.app/Contents/Resources/CaptureStart.wav
          test -f .build/YapOps.app/Contents/Resources/CaptureEnd.wav
          plutil -lint .build/YapOps.app/Contents/Info.plist
          codesign --verify --deep --strict .build/YapOps.app
